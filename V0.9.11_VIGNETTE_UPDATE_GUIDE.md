# v0.9.11 Status - Vignette Updates Required

**Date**: 2025-11-25  
**Version**: 0.9.11  
**Status**: IN PROGRESS

## Completed

✅ Version bumped to 0.9.11  
✅ Plan created (`VERSION_0.9.11_PLAN.md`)  
✅ Git committed

## Vignette Update Plan

### getting-started.Rmd

**File**: `/Users/frkkan96/Documents/src/pladdrr/vignettes/getting-started.Rmd`

**Required Changes** (systematic replacements):

1. **Introduction** (Line 23):
   ```markdown
   # Old
   clean S3 interface
   
   # New  
   clean R6 object-oriented interface
   ```

2. **Sound Generation** (Lines 47-48):
   ```r
   # Old (works but S3 result)
   sound_a4 <- generate_sine_wave(frequency = 440, duration = 0.5)
   
   # New (R6)
   sound_a4 <- generate_sine_wave(frequency = 440, duration = 0.5)
   # Note: generate_* functions can stay - they work with both
   ```

3. **Loading Sounds** (Lines 65-69):
   ```r
   # Old
   speech <- read_sound("path/to/speech.wav")
   speech_left <- read_sound("path/to/stereo.wav", channel = 0)
   
   # New
   speech <- Sound$new("path/to/speech.wav")
   speech_left <- Sound$new("path/to/stereo.wav", channel = 1)  # Note: 1-indexed
   ```

4. **Sound Properties** (Lines 77-88):
   ```r
   # Old
   get_duration(sound_a4)
   get_sampling_rate(sound_a4)
   get_n_samples(sound_a4)
   get_n_channels(sound_a4)
   
   # New
   sound_a4$get_duration()
   sound_a4$get_sampling_frequency()
   sound_a4$get_number_of_samples()
   sound_a4$get_number_of_channels()
   ```

5. **Sound Statistics** (Lines 96-103):
   ```r
   # These functions work with R6, no change needed
   sound_mean(sound_a4)
   sound_rms(sound_a4)
   # etc.
   ```

6. **Pitch Analysis** (Lines 112-118):
   ```r
   # Old
   pitch <- extract_pitch(speech, pitch_floor = 75, pitch_ceiling = 600)
   mean_f0 <- get_mean_pitch(pitch)
   min_f0 <- get_min_pitch(pitch)
   max_f0 <- get_max_pitch(pitch)
   
   # New
   pitch <- speech$to_pitch(pitch_floor = 75, pitch_ceiling = 600)
   mean_f0 <- pitch$get_mean()
   min_f0 <- pitch$get_minimum()
   max_f0 <- pitch$get_maximum()
   ```

7. **Formant Analysis** (~Line 150):
   ```r
   # Old
   formants <- extract_formants(sound, max_formant = 5500, n_formants = 5)
   summary(formants)  # THIS FAILS with R6
   
   # New
   formants <- sound$to_formant_burg(max_frequency = 5500, max_formants = 5)
   formants  # R6 objects have print methods
   # Or convert to data frame:
   formant_df <- formants$as_data_frame()
   ```

8. **Intensity Analysis** (~Line 120+):
   ```r
   # Old
   intensity <- extract_intensity(sound)
   mean_int <- get_mean_intensity(intensity)
   
   # New
   intensity <- sound$to_intensity()
   mean_int <- intensity$get_mean()
   ```

### Systematic Replacement Script

For automated conversion, use these patterns:

```bash
# In vignettes/getting-started.Rmd

# Sound loading
sed -i.bak 's/read_sound(\([^)]*\))/Sound$new(\1)/g'

# Sound properties  
sed -i.bak 's/get_duration(\([^)]*\))/\1$get_duration()/g'
sed -i.bak 's/get_sampling_rate(\([^)]*\))/\1$get_sampling_frequency()/g'
sed -i.bak 's/get_n_samples(\([^)]*\))/\1$get_number_of_samples()/g'
sed -i.bak 's/get_n_channels(\([^)]*\))/\1$get_number_of_channels()/g'

# Pitch
sed -i.bak 's/extract_pitch(\([^)]*\))/\1$to_pitch()/g'
sed -i.bak 's/get_mean_pitch(\([^)]*\))/\1$get_mean()/g'
sed -i.bak 's/get_min_pitch(\([^)]*\))/\1$get_minimum()/g'
sed -i.bak 's/get_max_pitch(\([^)]*\))/\1$get_maximum()/g'

# Formants
sed -i.bak 's/extract_formants/to_formant_burg/g'

# summary() calls - need manual review
# Remove or replace with print or as_data_frame()
```

## Recommendation

Due to the complexity and potential for errors with automated replacements:

### Option A: Manual Update (Recommended)
1. Open `vignettes/getting-started.Rmd`
2. Search and replace S3 patterns systematically
3. Test after each section
4. Verify examples run

### Option B: Use Automated Script with Review
1. Run sed commands above
2. Review all changes carefully
3. Fix any regex errors
4. Test thoroughly

### Option C: Defer to Future PR
Since v0.9.10 already has:
- ✅ Full R6 implementation
- ✅ Deprecated S3 wrappers
- ✅ Build workaround (`--no-build-vignettes`)

Could defer vignette updates to a separate PR focused solely on documentation.

## Current Workaround

Package builds successfully with:
```bash
R CMD build --no-build-vignettes .
```

Vignettes can be built separately after updates.

## Next Actions

Choose approach and update vignettes, OR proceed to v1.0.0 planning.

---

**Estimated Time**: 1-2 hours for careful manual updates  
**Risk**: Low if done carefully, Medium if automated  
**Urgency**: Medium (package works, vignettes are bonus)
