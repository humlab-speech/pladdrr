% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cepstrum_plots.R
\name{plot_cpp_timeseries}
\alias{plot_cpp_timeseries}
\title{Plot CPP Time Series}
\usage{
plot_cpp_timeseries(
  cepstrogram,
  time_range = NULL,
  qmin = 0.001,
  qmax = 0,
  n_samples = 100,
  smooth = FALSE,
  smooth_span = 0.1,
  reference_lines = NULL,
  title = NULL,
  theme = c("minimal", "bw", "classic")
)
}
\arguments{
\item{cepstrogram}{PowerCepstrogram object}

\item{time_range}{Numeric vector. c(start, end) time range to display (default: NULL = auto)}

\item{qmin}{Numeric. Minimum quefrency for peak search (default: 0.001)}

\item{qmax}{Numeric. Maximum quefrency for peak search (default: 0)}

\item{n_samples}{Integer. Number of time points to sample (default: 100)}

\item{smooth}{Logical. Apply smoothing to CPP contour (default: FALSE)}

\item{smooth_span}{Numeric. Smoothing span for loess (default: 0.1)}

\item{reference_lines}{Numeric vector. Reference CPP values to plot as horizontal lines (default: NULL)}

\item{title}{Character. Plot title (default: auto-generated)}

\item{theme}{Character. ggplot2 theme (default: "minimal")}
}
\value{
A ggplot2 object
}
\description{
Creates a line plot of Cepstral Peak Prominence (CPP) values over time
from a PowerCepstrogram object. Useful for tracking voice quality variation.
}
\examples{
\dontrun{
sound <- Sound$new("voice.wav")
cepstrogram <- sound$to_powercepstrogram(pitch_floor = 60, time_step = 0.002)

# CPP time series
plot_cpp_timeseries(cepstrogram)

# With smoothing and reference lines
plot_cpp_timeseries(cepstrogram, 
                   smooth = TRUE,
                   reference_lines = c(5, 10, 15))
}

}
