% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-ops.R
\name{sound_extract_and_pitch}
\alias{sound_extract_and_pitch}
\title{Extract Parts and Analyze Pitch in Single C++ Call}
\usage{
sound_extract_and_pitch(
  sound,
  from_times,
  to_times,
  time_step = 0,
  pitch_floor = 75,
  pitch_ceiling = 600,
  return_r6 = TRUE
)
}
\arguments{
\item{sound}{Sound object (R6) or external pointer}

\item{from_times}{Numeric vector of start times}

\item{to_times}{Numeric vector of end times}

\item{time_step}{Numeric. Pitch time step (0 = automatic)}

\item{pitch_floor}{Numeric. Pitch floor in Hz (default: 75)}

\item{pitch_ceiling}{Numeric. Pitch ceiling in Hz (default: 600)}

\item{return_r6}{Logical. Return R6 Pitch objects (TRUE) or raw xptrs (FALSE)}
}
\value{
List of Pitch objects (R6 or xptr depending on return_r6)
}
\description{
The most efficient way to analyze multiple intervals from a sound.
Combines extraction and analysis in a single C++ call, avoiding all
intermediate R6 object creation.
}
\examples{
\dontrun{
# Instead of the slow pattern:
# parts <- lapply(1:n, function(i) sound$extract_part(from[i], to[i]))
# pitches <- lapply(parts, function(p) p$to_pitch())

# Use combined operation:
pitches <- sound_extract_and_pitch(sound, from_times, to_times)
}

}
