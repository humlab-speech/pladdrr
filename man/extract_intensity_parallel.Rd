% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel-batch.R
\name{extract_intensity_parallel}
\alias{extract_intensity_parallel}
\title{Parallel Intensity Extraction}
\usage{
extract_intensity_parallel(
  files,
  n_cores = NULL,
  minimum_pitch = 100,
  time_step = 0
)
}
\arguments{
\item{files}{Character vector of file paths}

\item{n_cores}{Integer. Number of cores (default: auto)}

\item{minimum_pitch}{Numeric. Minimum pitch for analysis (default: 100)}

\item{time_step}{Numeric. Time step (default: 0, auto)}
}
\value{
List of Intensity objects
}
\description{
Extract intensity from multiple files in parallel.
}
\examples{
audio_dir <- tempfile("audio_")
dir.create(audio_dir)
tone <- Sound$create_tone(frequency = 220, duration = 0.3, sampling_rate = 16000)
tone$save(file.path(audio_dir, "tone1.wav"))
files <- list.files(audio_dir, pattern = "\\\\.wav$", full.names = TRUE)

# n_cores = 1 keeps this a single-process example (CRAN-safe)
intensities <- extract_intensity_parallel(files, n_cores = 1)

}
