% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-analysis.R
\name{voice_quality_batch}
\alias{voice_quality_batch}
\title{Voice Quality Batch Analysis}
\usage{
voice_quality_batch(
  sound,
  time_step = 0,
  pitch_floor = 75,
  pitch_ceiling = 600,
  periods_per_window = 3,
  max_n_candidates = 15L,
  very_accurate = FALSE,
  silence_threshold = 0.03,
  voicing_threshold = 0.45,
  octave_cost = 0.01,
  octave_jump_cost = 0.35,
  voiced_unvoiced_cost = 0.14,
  minimum_pitch_intensity = 100,
  from_time = 0,
  to_time = 0
)
}
\arguments{
\item{sound}{A Sound object}

\item{time_step}{Time step for pitch and intensity analysis (default 0.0 = auto)}

\item{pitch_floor}{Minimum pitch in Hz (default 75.0)}

\item{pitch_ceiling}{Maximum pitch in Hz (default 600.0)}

\item{periods_per_window}{Periods per window for pitch (default 3.0)}

\item{max_n_candidates}{Maximum number of pitch candidates (default 15)}

\item{very_accurate}{Use accurate but slow pitch algorithm (default FALSE)}

\item{silence_threshold}{Silence threshold (default 0.03)}

\item{voicing_threshold}{Voicing threshold (default 0.45)}

\item{octave_cost}{Cost per octave jump (default 0.01)}

\item{octave_jump_cost}{Cost per octave jump (default 0.35)}

\item{voiced_unvoiced_cost}{Cost for voiced/unvoiced transition (default 0.14)}

\item{minimum_pitch_intensity}{Minimum intensity for pitch (default 100.0)}

\item{from_time}{Start time for statistics (default 0.0 = start)}

\item{to_time}{End time for statistics (default 0.0 = end)}
}
\value{
List containing:
  \describe{
    \item{pitch}{List with mean, maximum, minimum, stdev, median (all in Hz)}
    \item{intensity}{List with mean, maximum, minimum, stdev, median (all in dB)}
  }
}
\description{
Efficiently extracts common voice quality measures in a single C++ call,
avoiding multiple R<->C++ boundary crossings. This function is optimized
for typical voice quality workflows (DSI, AVQI, etc.).
}
\details{
This function reduces 10 R<->C++ boundary crossings to just 1 call:
\itemize{
  \item sound$to_pitch_cc() -> 1 call
  \item pitch$get_mean(), get_maximum(), get_minimum(), get_standard_deviation() -> 4 calls
  \item sound$to_intensity() -> 1 call
  \item intensity$get_mean(), get_maximum(), get_minimum(), get_standard_deviation() -> 4 calls
}

Expected speedup: 15-20\% compared to individual method calls
}
\examples{
\dontrun{
sound <- Sound("vowel.wav")

# Traditional approach (10 R<->C++ calls)
pitch <- sound$to_pitch_cc()
pitch_mean <- pitch$get_mean(0, 0, "Hz")
pitch_max <- pitch$get_maximum(0, 0, "Hz")
# ... etc

# Batch approach (1 R<->C++ call)
vq <- voice_quality_batch(sound)
pitch_mean <- vq$pitch$mean
pitch_max <- vq$pitch$maximum
}

}
