% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/praat-direct.R
\name{to_pitch_ac_direct}
\alias{to_pitch_ac_direct}
\title{Create Pitch from Sound Directly (Autocorrelation) - Full Parameters}
\usage{
to_pitch_ac_direct(
  sound,
  time_step = 0,
  pitch_floor = 75,
  pitch_ceiling = 600,
  max_candidates = 15,
  very_accurate = FALSE,
  silence_threshold = 0.03,
  voicing_threshold = 0.45,
  octave_cost = 0.01,
  octave_jump_cost = 0.35,
  voiced_unvoiced_cost = 0.14,
  max_number_of_candidates = NULL
)
}
\arguments{
\item{sound}{Sound object or external pointer}

\item{time_step}{Time step (0 = auto, typically 0.75/pitch_floor)}

\item{pitch_floor}{Minimum pitch (Hz, default 75)}

\item{pitch_ceiling}{Maximum pitch (Hz, default 600)}

\item{max_candidates}{Maximum number of pitch candidates (default 15)}

\item{very_accurate}{Use accurate but slower method (default FALSE)}

\item{silence_threshold}{Frames below this relative intensity are unvoiced (default 0.03)}

\item{voicing_threshold}{Strength required for voiced decision (default 0.45)}

\item{octave_cost}{Cost per octave in path finding (default 0.01)}

\item{octave_jump_cost}{Cost for octave jumps (default 0.35)}

\item{voiced_unvoiced_cost}{Cost for voicing transitions (default 0.14)}
}
\value{
External pointer to Pitch (NOT R6 object)
}
\description{
Create Pitch analysis using autocorrelation method with full control over
all voicing parameters. Returns raw external pointer for maximum performance.

**NEW in v4.0.1:** Exposes all voicing parameters that were previously only
available in Tier 1 (Standard) API.
}
\examples{
\dontrun{
sound <- Sound("speech.wav")

# With custom voicing threshold (stricter voicing detection)
pitch_ptr <- to_pitch_ac_direct(sound, voicing_threshold = 0.6)

# With custom silence threshold (more sensitive)
pitch_ptr <- to_pitch_ac_direct(sound, silence_threshold = 0.01)

# Use with query functions
f0 <- get_pitch_value_direct(pitch_ptr, 1.0, "hertz", TRUE)
}

}
