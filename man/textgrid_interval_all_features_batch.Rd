% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{textgrid_interval_all_features_batch}
\alias{textgrid_interval_all_features_batch}
\title{Extract All Acoustic Features for TextGrid Intervals (Batch, SIMD)}
\usage{
textgrid_interval_all_features_batch(
  textgrid_xptr,
  pitch_xptr = NULL,
  formant_xptr = NULL,
  intensity_xptr = NULL,
  tier_number = 1L
)
}
\arguments{
\item{textgrid_xptr}{External pointer to TextGrid}

\item{pitch_xptr}{External pointer to Pitch (optional)}

\item{formant_xptr}{External pointer to Formant (optional)}

\item{intensity_xptr}{External pointer to Intensity (optional)}

\item{tier_number}{Tier number (1-based)}
}
\value{
Data frame with all available features per interval
}
\description{
Comprehensive batch extraction of pitch, formant F1/F2, and intensity
statistics for all intervals in a single call, as an alternative to
calling `textgrid_interval_pitch_batch()`, `textgrid_interval_formant_batch()`,
and `textgrid_interval_intensity_batch()` separately.
}
\details{
Duration calculation and statistics aggregation use SIMD where
applicable.
}
\examples{
sound <- Sound$create_tone(frequency = 180, duration = 1.0)
pitch <- sound$to_pitch()
formant <- sound$to_formant_burg()
intensity <- sound$to_intensity()
tg <- textgrid_create(0, 1, "phones")
tg$insert_boundary("phones", 0.5)
tg$set_interval_text("phones", 1, "a")
tg$set_interval_text("phones", 2, "b")

stats <- textgrid_interval_all_features_batch(
  tg$.xptr, pitch$.xptr, formant$.xptr, intensity$.xptr, tier_number = 1
)

}
