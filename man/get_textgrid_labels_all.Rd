% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/textgrid-batch.R
\name{get_textgrid_labels_all}
\alias{get_textgrid_labels_all}
\title{Get All Labels from TextGrid Tier (Batch)}
\usage{
get_textgrid_labels_all(textgrid, tier)
}
\arguments{
\item{textgrid}{A TextGrid object}

\item{tier}{Tier number (1-based) or tier name}
}
\value{
Character vector of all interval labels
}
\description{
Extract all interval labels from a tier in a single operation, instead of
calling `textgrid$get_interval_text()` repeatedly.
}
\examples{
\dontrun{
tg <- TextGrid("example.TextGrid")
labels <- get_textgrid_labels_all(tg, tier = 1)
table(labels)  # Frequency table of labels
}

}
