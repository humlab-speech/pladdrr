% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel-batch.R
\name{process_sounds_parallel}
\alias{process_sounds_parallel}
\title{Batch Process Sounds in Parallel}
\usage{
process_sounds_parallel(
  sounds,
  analysis_func,
  n_cores = NULL,
  threads_per_worker = NULL,
  ...
)
}
\arguments{
\item{sounds}{List of Sound objects or external pointers}

\item{analysis_func}{Function. Analysis function to apply.
Should accept a Sound object/pointer and return results.}

\item{n_cores}{Integer. Number of CPU cores (default: auto)}

\item{threads_per_worker}{Integer or `NULL`. C++ threads each worker may use
for Praat kernels. `NULL` (default) auto-divides cores among workers to
avoid oversubscription; set `1` to force single-threaded workers.}

\item{...}{Additional arguments passed to analysis_func}
}
\value{
List of results
}
\description{
Apply analysis to pre-loaded Sound objects in parallel.
Use when sounds are already in memory.
}
\examples{
\donttest{
sounds <- list(
  Sound$create_tone(frequency = 220, duration = 0.2),
  Sound$create_tone(frequency = 440, duration = 0.2)
)

# Process using at most 2 cores (CRAN example policy)
results <- process_sounds_parallel(sounds, function(s) {
  pitch <- s$to_pitch()
  pitch$get_mean(0, 0, "hertz")
}, n_cores = 2)
}

}
