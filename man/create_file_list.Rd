% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-processing.R
\name{create_file_list}
\alias{create_file_list}
\title{Create File List (Replaces Praat's Strings Object)}
\usage{
create_file_list(
  directory,
  pattern = NULL,
  full_names = TRUE,
  recursive = FALSE
)
}
\arguments{
\item{directory}{Character path to directory}

\item{pattern}{Regular expression pattern to match files}

\item{full_names}{Logical, return full paths (default: TRUE)}

\item{recursive}{Logical, search recursively (default: FALSE)}
}
\value{
Character vector of file paths
}
\description{
Create a file list similar to Praat's "Create Strings as file list".
Returns a simple character vector of file paths.
}
\examples{
audio_dir <- tempfile("audio_")
dir.create(audio_dir)
tone <- Sound$create_tone(frequency = 150, duration = 0.3, sampling_rate = 16000)
tone$save(file.path(audio_dir, "tone1.wav"))

# Equivalent to: Create Strings as file list: "list", "*.wav"
wav_files <- create_file_list(audio_dir, pattern = "\\\\.wav$")

}
