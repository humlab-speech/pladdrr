% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formant.R
\name{extract_formants}
\alias{extract_formants}
\title{Extract formants from a sound object}
\usage{
extract_formants(
  sound,
  time_step = 0,
  max_formant = 5500,
  n_formants = 5,
  window_length = 0.025,
  pre_emphasis_from = 50
)
}
\arguments{
\item{sound}{A praat_sound object created by \code{\link{read_sound}} or
\code{\link{create_sound}}}

\item{time_step}{Time step in seconds for formant analysis (0 = auto: 4x Nyquist)}

\item{max_formant}{Maximum formant frequency in Hz (default: 5500 for adult female,
use 5000 for adult male, 8000 for child)}

\item{n_formants}{Number of formants to track (default: 5)}

\item{window_length}{Analysis window length in seconds (default: 0.025)}

\item{pre_emphasis_from}{Pre-emphasis frequency in Hz (default: 50)}
}
\value{
A praat_formant object (S3 class) with:
  \itemize{
    \item \code{values}: data.frame with columns time, formant_number, frequency, bandwidth
    \item \code{n_frames}: number of analysis frames
    \item \code{time_step}: actual time step used
    \item \code{max_formant}: maximum formant frequency setting
    \item \code{n_formants}: number of formants tracked
  }
}
\description{
Analyzes formant frequencies (vocal tract resonances) from a sound object
using Praat's Burg algorithm.
}
\examples{
\dontrun{
sound <- read_sound("vowel.wav")
formants <- extract_formants(sound, max_formant = 5500)

# Get F1 and F2 at specific time
f1 <- get_formant_at_time(formants, formant_number = 1, time = 0.5)
f2 <- get_formant_at_time(formants, formant_number = 2, time = 0.5)
}
}
