% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{pitch_get_statistics_batch}
\alias{pitch_get_statistics_batch}
\title{Batch get pitch statistics over multiple time intervals}
\usage{
pitch_get_statistics_batch(
  pitch_xptr,
  from_times,
  to_times,
  metrics,
  unit = 0L
)
}
\arguments{
\item{pitch_xptr}{External pointer to Pitch object}

\item{from_times}{Numeric vector of interval start times}

\item{to_times}{Numeric vector of interval end times}

\item{metrics}{Character vector of metrics: "min", "max", "mean", "stdev",
"q25", "q50" (median), "q75", "count_voiced"}

\item{unit}{Integer code for unit (0=HERTZ, 1=HERTZ_LOGARITHMIC, etc)}
}
\value{
NumericMatrix with intervals as rows, metrics as columns
}
\description{
Calculate multiple pitch statistics (min, max, mean, stdev, quantiles) over
multiple time intervals in a single C++ call, avoiding repeated R method
calls.
}
\examples{
sound <- Sound$create_tone(frequency = 150, duration = 1.0)
pitch <- sound$to_pitch()
pitch_get_statistics_batch(
  pitch$.xptr,
  from_times = c(0, 0.5),
  to_times = c(0.5, 1.0),
  metrics = c("min", "max", "mean")
)
}
\keyword{internal}
