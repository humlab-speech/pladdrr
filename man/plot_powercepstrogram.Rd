% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cepstrum_plots.R
\name{plot_powercepstrogram}
\alias{plot_powercepstrogram}
\title{Plot PowerCepstrogram}
\usage{
plot_powercepstrogram(
  cepstrogram,
  time_range = NULL,
  quefrency_range = c(0, 0.05),
  db_range = NULL,
  color_scale = c("viridis", "inferno", "magma", "plasma"),
  show_cpp_contour = FALSE,
  contour_color = "white",
  title = NULL,
  theme = c("minimal", "bw", "classic")
)
}
\arguments{
\item{cepstrogram}{PowerCepstrogram object}

\item{time_range}{Numeric vector. c(start, end) time range to display
(default: NULL = auto)}

\item{quefrency_range}{Numeric vector. c(min, max) quefrency range to display
(default: c(0, 0.05))}

\item{db_range}{Numeric vector. c(min, max) dB range for color scale
(default: NULL = auto)}

\item{color_scale}{Character. Color palette: "viridis", "inferno", "magma",
"plasma" (default: "viridis")}

\item{show_cpp_contour}{Logical. Overlay CPP contour over time (default:
FALSE)}

\item{contour_color}{Character. Color for CPP contour line (default: "white")}

\item{title}{Character. Plot title (default: auto-generated)}

\item{theme}{Character. ggplot2 theme (default: "minimal")}
}
\value{
A ggplot2 object
}
\description{
Creates a heatmap visualization of a power cepstrogram showing how
the cepstral spectrum varies over time, similar to a spectrogram.
}
\examples{
sound <- Sound$create_tone(frequency = 220, duration = 0.5)
cepstrogram <- sound$to_powercepstrogram(pitch_floor = 60, time_step = 0.002)

# Basic plot
plot_powercepstrogram(cepstrogram)

# With CPP contour
plot_powercepstrogram(cepstrogram,
                     show_cpp_contour = TRUE,
                     quefrency_range = c(0.001, 0.02))

}
