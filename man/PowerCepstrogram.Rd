% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/powercepstrum-r6.R
\name{PowerCepstrogram}
\alias{PowerCepstrogram}
\title{PowerCepstrogram Object - Rcpp Module Wrapper}
\usage{
PowerCepstrogram(.xptr = NULL)
}
\arguments{
\item{.xptr}{External pointer to C++ PowerCepstrogram object (internal use)}
}
\value{
PowerCepstrogram object with methods for CPPS analysis
}
\description{
Represents a time-varying power cepstrum (PowerCepstrogram) from Praat.
Uses Rcpp Modules for high-performance access to Praat's PowerCepstrogram object.

**Architecture:** Direct C++ method calls via Rcpp Modules, eliminating
R6 dispatch overhead for improved performance in CPPS calculations.
}
\details{
A PowerCepstrogram is a time-frequency representation of the power cepstrum,
similar to how a Spectrogram represents time-varying spectra. It allows
tracking of voice quality measures like CPP/CPPS across time.

## Creating PowerCepstrogram Objects

PowerCepstrogram objects are created from Sound objects:
- `sound$to_powercepstrogram()` - Compute time-varying power cepstrum

## Query Methods

- `$get_cpps()` - Get CPPS (Smoothed Cepstral Peak Prominence) - CRITICAL FOR AVQI
- `$get_cpp_at_time()` - Get CPP at specific time
- `$get_xmin()`, `$get_xmax()`, `$get_duration()` - Time domain properties
- `$get_ymin()`, `$get_ymax()` - Quefrency domain properties

## Transformation

- `$get_slice()` - Extract PowerCepstrum slice at time
- `$smooth()` - Smooth in time and quefrency dimensions
- `$to_matrix()` - Convert to Matrix object

## Export

- `$as_matrix()` - Export as R numeric matrix (quefrency × time)
}
\examples{
\dontrun{
# Analyze CPPS over time
sound <- Sound$new("voice.wav")
pcep <- sound$to_powercepstrogram(pitch_floor = 60)

# Get CPPS (critical for AVQI)
cpps <- pcep$get_cpps(subtract_tilt = FALSE)
print(paste("CPPS:", round(cpps, 2), "dB"))
}

}
