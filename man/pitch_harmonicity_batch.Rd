% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-analysis.R
\name{pitch_harmonicity_batch}
\alias{pitch_harmonicity_batch}
\title{Pitch and Harmonicity Combined Batch Analysis}
\usage{
pitch_harmonicity_batch(
  sound,
  time_step = 0.01,
  pitch_floor = 75,
  pitch_ceiling = 600,
  silence_threshold = 0.1,
  voicing_threshold = 0.45,
  from_time = 0,
  to_time = 0
)
}
\arguments{
\item{sound}{A Sound object}

\item{time_step}{Time step for analysis (default 0.01)}

\item{pitch_floor}{Minimum pitch in Hz (default 75.0)}

\item{pitch_ceiling}{Maximum pitch in Hz (default 600.0)}

\item{silence_threshold}{Silence threshold (default 0.1)}

\item{voicing_threshold}{Voicing threshold (default 0.45)}

\item{from_time}{Start time for statistics (default 0.0 = start)}

\item{to_time}{End time for statistics (default 0.0 = end)}
}
\value{
List containing:
  \describe{
    \item{pitch}{List with mean, maximum, minimum, stdev, median (all in Hz)}
    \item{hnr}{List with mean, stdev, median (all in dB)}
  }
}
\description{
Efficiently extracts both Pitch and Harmonicity (HNR) statistics by sharing
the autocorrelation computation between them.
}
\details{
This function is more efficient than calling sound$to_pitch() and
sound$to_harmonicity() separately, as both use autocorrelation which
can be computed once and shared.

Expected speedup: 10-15\% compared to separate analyses
}
\examples{
\dontrun{
sound <- Sound("vowel.wav")

# Batch approach (1 R<->C++ call)
result <- pitch_harmonicity_batch(sound)
pitch_mean <- result$pitch$mean
hnr_mean <- result$hnr$mean
}

}
