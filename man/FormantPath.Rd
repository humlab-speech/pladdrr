% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formantpath-module.R
\name{FormantPath}
\alias{FormantPath}
\title{Create a FormantPath object from a Sound}
\usage{
FormantPath(
  sound,
  time_step = 0.005,
  max_num_formants = 5,
  formant_ceiling = 5500,
  window_length = 0.025,
  preemphasis_from = 50,
  ceiling_step_fraction = 0.05,
  num_steps_up_down = 4L
)
}
\arguments{
\item{sound}{A Sound object or path to audio file}

\item{time_step}{Time step for analysis in seconds (must be > 0, typically 0.005)}

\item{max_num_formants}{Maximum number of formants to track (typically 5)}

\item{formant_ceiling}{Maximum formant frequency in Hz (typically 5000-5500)}

\item{window_length}{Analysis window length in seconds (typically 0.025)}

\item{preemphasis_from}{Preemphasis frequency in Hz (typically 50)}

\item{ceiling_step_fraction}{Step size for ceiling frequency variation (0.05-0.1)}

\item{num_steps_up_down}{Number of steps above/below ceiling (typically 2-4)}
}
\value{
FormantPath object with S3 class
}
\description{
A FormantPath object represents multiple formant tracking candidates with
different ceiling frequencies, allowing for robust formant analysis by
automatically selecting the optimal tracking path.
}
\examples{
\dontrun{
sound <- Sound("vowel.wav")
fp <- FormantPath(sound, max_num_formants = 5, formant_ceiling = 5500)

# Get number of candidate tracks
fp$get_number_of_candidates()

# Extract optimal formant
formant <- fp$extract_formant()

# Export to data frame
df <- as.data.frame(fp)
}
}
