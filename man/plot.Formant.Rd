% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-methods.R
\name{plot.Formant}
\alias{plot.Formant}
\title{Plot Formant Tracks}
\usage{
\method{plot}{Formant}(
  x,
  from_time = NULL,
  to_time = NULL,
  max_formant = 3,
  garnish = TRUE,
  title = "Formant",
  colors = NULL,
  ...
)
}
\arguments{
\item{x}{Formant object}

\item{from_time}{Start time in seconds (NULL = from beginning)}

\item{to_time}{End time in seconds (NULL = to end)}

\item{max_formant}{Maximum formant number to display (default: 3)}

\item{garnish}{Logical. Add axis labels and title (default: TRUE)}

\item{title}{Character. Plot title (default: "Formant")}

\item{colors}{Character vector. Colors for each formant (default: auto)}

\item{...}{Additional arguments (currently unused)}
}
\value{
A ggplot2 object
}
\description{
Creates a formant trajectory visualization showing F1, F2, F3, etc.
}
\examples{
sound <- Sound$create_tone(frequency = 220, duration = 0.5)
formant <- sound$to_formant_burg()

# Basic plot
plot(formant)

# Show first 5 formants
plot(formant, max_formant = 5)

# Customize
plot(formant, max_formant = 2,
     colors = c("red", "blue"))

}
