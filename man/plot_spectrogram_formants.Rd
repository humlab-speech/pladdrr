% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-combined.R
\name{plot_spectrogram_formants}
\alias{plot_spectrogram_formants}
\title{Plot Spectrogram with Formant Overlay}
\usage{
plot_spectrogram_formants(
  spectrogram,
  formant,
  from_time = NULL,
  to_time = NULL,
  max_formant = 3,
  formant_colors = NULL,
  dynamic_range = 70,
  title = "Spectrogram + Formants",
  ...
)
}
\arguments{
\item{spectrogram}{Spectrogram object}

\item{formant}{Formant object}

\item{from_time}{Start time in seconds (NULL = from beginning)}

\item{to_time}{End time in seconds (NULL = to end)}

\item{max_formant}{Maximum formant number to display (default: 3)}

\item{formant_colors}{Character vector. Colors for formants (default: auto)}

\item{dynamic_range}{Numeric. Spectrogram dynamic range in dB (default: 70)}

\item{title}{Character. Plot title (default: "Spectrogram + Formants")}

\item{...}{Additional arguments passed to plot.Spectrogram}
}
\value{
A ggplot object with formant tracks overlaid on spectrogram
}
\description{
Creates a spectrogram heatmap with formant trajectories overlaid.
This is a common visualization pattern in Praat for vowel analysis.
}
\examples{
sound <- Sound$create_tone(frequency = 220, duration = 0.5)
spectrogram <- sound$to_spectrogram()
formant <- sound$to_formant_burg()

# Combined plot
plot_spectrogram_formants(spectrogram, formant)

# Show F1-F5
plot_spectrogram_formants(spectrogram, formant, max_formant = 5)

}
