% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formant.R
\name{.lpc_to_formants}
\alias{.lpc_to_formants}
\title{Convert LPC coefficients to formants}
\usage{
.lpc_to_formants(frame, sr, lpc_order, n_formants, max_formant)
}
\arguments{
\item{frame}{Numeric vector, one windowed analysis frame}

\item{sr}{Sampling rate in Hz}

\item{lpc_order}{LPC order (number of coefficients)}

\item{n_formants}{Number of formants to return}

\item{max_formant}{Maximum formant frequency (Hz)}
}
\value{
A data.frame with columns \code{frequency} and \code{bandwidth},
  one row per formant, padded with \code{NA} if fewer roots were found
  than \code{n_formants}
}
\description{
Convert LPC coefficients to formants
}
\examples{
set.seed(1)
frame <- sin(2 * pi * 500 * seq(0, 0.025, length.out = 400)) + rnorm(400, sd = 0.01)
pladdrr:::.lpc_to_formants(frame, sr = 16000, lpc_order = 12,
                            n_formants = 4, max_formant = 5500)
}
\keyword{internal}
