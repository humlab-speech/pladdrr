% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.calculate_cpps_ultra_cpp}
\alias{.calculate_cpps_ultra_cpp}
\title{Calculate CPPS in single optimized C++ call (Tier 4 Ultra)}
\usage{
.calculate_cpps_ultra_cpp(
  sound_xptr,
  time_averaging_window = 0.01,
  quefrency_averaging_window = 0.001,
  pitch_floor = 60,
  pitch_ceiling = 330,
  subtract_trend = TRUE,
  time_step = 0.002,
  max_quefrency = 0.05,
  tolerance = 0.05,
  interpolation = 1L,
  tilt_line_quefrency = 0.001,
  line_type = 2L,
  fit_method = 1L,
  pre_emphasis_from = 50,
  max_frequency = 5000
)
}
\arguments{
\item{sound_xptr}{External pointer to Sound object}

\item{time_averaging_window}{Time averaging window in seconds (default 0.01)}

\item{quefrency_averaging_window}{Quefrency averaging window in seconds (default 0.001)}

\item{pitch_floor}{Minimum F0 in Hz (default 60)}

\item{pitch_ceiling}{Maximum F0 in Hz (default 330)}

\item{subtract_trend}{Subtract tilt before smoothing (default TRUE)}

\item{time_step}{Time step for cepstrogram in seconds (default 0.002)}

\item{max_quefrency}{Maximum quefrency in seconds (default 0.05)}

\item{tolerance}{Tolerance for peak detection (default 0.05)}

\item{interpolation}{Peak interpolation method (0=none, 1=parabolic, 2=cubic, 3=sinc70, 4=sinc700)}

\item{tilt_line_quefrency}{Quefrency for tilt line in seconds (default 0.001)}

\item{line_type}{Trend line type (1=straight, 2=exponential decay)}

\item{fit_method}{Fitting method (1=robust fast, 2=least squares, 3=robust slow)}

\item{pre_emphasis_from}{Pre-emphasis frequency in Hz (default 50.0) - CRITICAL for correct CPPS}

\item{max_frequency}{Maximum frequency for cepstrogram in Hz (default 5000.0)}
}
\value{
Single CPPS value in dB
}
\description{
Consolidates PowerCepstrogram creation + CPPS extraction in a single C++ call.
Eliminates R/C++ boundary crossings and reduces parameter overhead.
2-3x faster than calculate_cpps_fast() for AVQI applications.
}
\keyword{internal}
