% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-combined.R
\name{plot_textgrid_pitch}
\alias{plot_textgrid_pitch}
\title{Plot TextGrid with Pitch Contour}
\usage{
plot_textgrid_pitch(
  textgrid,
  pitch,
  tier = NULL,
  from_time = NULL,
  to_time = NULL,
  pitch_color = "darkgreen",
  tier_colors = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{textgrid}{TextGrid object}

\item{pitch}{Pitch object}

\item{tier}{Integer or character. Tier number or name to display (default: all)}

\item{from_time}{Start time in seconds (NULL = from beginning)}

\item{to_time}{End time in seconds (NULL = to end)}

\item{pitch_color}{Character. Pitch line color (default: "darkgreen")}

\item{tier_colors}{Character vector. Colors for each tier (default: auto)}

\item{title}{Character. Plot title (default: NULL)}

\item{...}{Additional arguments passed to plot methods}
}
\value{
A combined ggplot object
}
\description{
Creates a combined visualization showing a pitch contour with TextGrid
annotation tiers. This replicates Praat's TextGrid_Pitch_draw() function.
}
\examples{
if (requireNamespace("patchwork", quietly = TRUE) ||
    requireNamespace("gridExtra", quietly = TRUE)) {
  sound <- Sound$create_tone(frequency = 220, duration = 1.0)
  pitch <- sound$to_pitch()
  textgrid <- TextGrid$create(0, 1, "phonemes")
  textgrid$set_interval_text("phonemes", 1, "a")

  # Combined pitch + TextGrid
  plot_textgrid_pitch(textgrid, pitch)

  # Single tier
  plot_textgrid_pitch(textgrid, pitch, tier = "phonemes")
}

}
