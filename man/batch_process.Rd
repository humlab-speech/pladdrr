% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-processing.R, R/batch_processing.R
\name{batch_process}
\alias{batch_process}
\title{Batch Process Audio Files}
\usage{
batch_process(
  directory,
  pattern = "\\\\.wav$",
  func,
  recursive = FALSE,
  parallel = FALSE,
  ncores = parallel::detectCores() - 1,
  progress = TRUE,
  return_type = c("data.frame", "list", "bind_rows"),
  ...
)

batch_process(
  directory,
  pattern = "\\\\.wav$",
  func,
  recursive = FALSE,
  parallel = FALSE,
  ncores = parallel::detectCores() - 1,
  progress = TRUE,
  return_type = c("data.frame", "list", "bind_rows"),
  ...
)
}
\arguments{
\item{directory}{Character. Directory containing audio files.}

\item{pattern}{Character. File pattern to match (default: "\\\\.wav$").}

\item{func}{Function. Processing function that takes a Sound object and returns results.}

\item{recursive}{Logical. Search recursively in subdirectories (default: FALSE).}

\item{parallel}{Logical. Use parallel processing (default: FALSE).}

\item{ncores}{Integer. Number of cores for parallel processing (default: detectCores() - 1).}

\item{progress}{Logical. Show progress bar (default: TRUE).}

\item{return_type}{Character. How to combine results: "list", "data.frame", or "bind_rows" (default: "data.frame").}

\item{...}{Additional arguments passed to func.}

\item{cores}{Integer, number of cores for parallel processing (default: NULL = all-1)}
}
\value{
Data frame with results from all files

Results combined according to return_type parameter.
}
\description{
Process multiple audio files with a custom function. This replaces Praat's
pattern of creating Strings objects and looping over files.

Process multiple audio files with a user-defined function, similar to Praat's
file list iteration but using R's functional programming paradigm.
}
\examples{
\dontrun{
# Extract pitch statistics from all WAV files
results <- batch_process(
  directory = "audio_files/",
  pattern = "\\\\.wav$",
  func = function(sound) {
    pitch <- sound$to_pitch()
    list(
      mean_f0 = pitch$get_mean(from_time = 0, to_time = 0, unit = "hertz"),
      sd_f0 = pitch$get_standard_deviation(from_time = 0, to_time = 0, unit = "hertz")
    )
  }
)
}

\dontrun{
# Extract pitch statistics from all files
results <- batch_process(
  directory = "~/audio/",
  pattern = "\\\\.wav$",
  func = function(sound) {
    pitch <- sound$to_pitch(time_step = 0.01, pitch_floor = 75, pitch_ceiling = 600)
    data.frame(
      file = sound$get_name(),
      mean_f0 = pitch$get_mean(from_time = 0, to_time = 0, unit = "hertz"),
      sd_f0 = pitch$get_standard_deviation(from_time = 0, to_time = 0, unit = "hertz")
    )
  }
)
}

}
