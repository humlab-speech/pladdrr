% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discriminant-wrapper.R
\name{discriminant_from_matrix}
\alias{discriminant_from_matrix}
\title{Create Discriminant Analysis from labeled data}
\usage{
discriminant_from_matrix(data, labels)
}
\arguments{
\item{data}{Numeric matrix where rows are observations and columns are variables}

\item{labels}{Character vector of group labels (one per row in data)}
}
\value{
A Discriminant object
}
\description{
Performs Linear Discriminant Analysis on a labeled numeric matrix.
}
\examples{
\dontrun{
# Vowel formant data with labels
vowels <- matrix(c(
  700, 1200, 2500,  # /a/
  350, 2100, 2800,  # /i/
  450, 700, 2400    # /u/
), ncol = 3, byrow = TRUE)

labels <- c("a", "i", "u")
lda <- discriminant_from_matrix(vowels, labels)
lda$get_wilks_lambda(1)
}

}
