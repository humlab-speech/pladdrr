% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-processing.R
\name{pair_files}
\alias{pair_files}
\title{Pair Sound and TextGrid Files}
\usage{
pair_files(
  sound_dir,
  textgrid_dir = sound_dir,
  sound_pattern = "\\\\.wav$",
  textgrid_pattern = "\\\\.TextGrid$",
  by = "basename",
  require_both = TRUE
)
}
\arguments{
\item{sound_dir}{Character. Directory containing sound files.}

\item{textgrid_dir}{Character. Directory containing TextGrid files (default:
same as sound_dir).}

\item{sound_pattern}{Character. Pattern for sound files (default:
"\\\\.wav$").}

\item{textgrid_pattern}{Character. Pattern for TextGrid files (default:
"\\\\.TextGrid$").}

\item{by}{Character. Matching strategy: "basename" (default), "exact", or a
custom function.}

\item{require_both}{Logical. Require both files to exist (default: TRUE).}
}
\value{
Data frame with columns: sound_file, textgrid_file, basename.
}
\description{
Find matching pairs of Sound and TextGrid files based on filename matching.
Equivalent to Praat's manual file pairing but automated.
}
\examples{
audio_dir <- tempfile("audio_")
dir.create(audio_dir)
Sound$create_tone(frequency = 150, duration = 0.3, sampling_rate =
 16000)$save(
  file.path(audio_dir, "utt1.wav")
)
tg <- TextGrid$create(0, 0.3, "words")
tg$save(file.path(audio_dir, "utt1.TextGrid"))

# Find all matching pairs
pairs <- pair_files(sound_dir = audio_dir)

# Process each pair
results <- lapply(seq_len(nrow(pairs)), function(i) {
  sound <- Sound(pairs$sound_file[i])
  textgrid <- TextGrid(pairs$textgrid_file[i])
  # ... analysis ...
})

}
