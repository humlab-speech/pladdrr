% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-methods.R
\name{plot.Spectrogram}
\alias{plot.Spectrogram}
\title{Plot Spectrogram Heatmap}
\usage{
\method{plot}{Spectrogram}(
  x,
  from_time = NULL,
  to_time = NULL,
  from_freq = NULL,
  to_freq = NULL,
  garnish = TRUE,
  title = "Spectrogram",
  dynamic_range = 70,
  preemphasis = 50,
  ...
)
}
\arguments{
\item{x}{Spectrogram object}

\item{from_time}{Start time in seconds (NULL = from beginning)}

\item{to_time}{End time in seconds (NULL = to end)}

\item{from_freq}{Start frequency in Hz (NULL = from 0)}

\item{to_freq}{End frequency in Hz (NULL = to Nyquist)}

\item{garnish}{Logical. Add axis labels and title (default: TRUE)}

\item{title}{Character. Plot title (default: "Spectrogram")}

\item{dynamic_range}{Numeric. Dynamic range in dB (default: 70)}

\item{preemphasis}{Numeric. Pre-emphasis from Hz (default: 50)}

\item{...}{Additional arguments (currently unused)}
}
\value{
A ggplot2 object
}
\description{
Creates a time-frequency heatmap visualization of a Spectrogram.
}
\examples{
sound <- Sound$create_tone(frequency = 220, duration = 0.5)
spectrogram <- sound$to_spectrogram()

# Basic plot
plot(spectrogram)

# Focus on speech range
plot(spectrogram, to_freq = 5000)

}
