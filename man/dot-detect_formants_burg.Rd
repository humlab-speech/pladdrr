% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formant.R
\name{.detect_formants_burg}
\alias{.detect_formants_burg}
\title{Internal formant detection using Burg's method}
\usage{
.detect_formants_burg(
  signal,
  sr,
  time_step,
  max_formant,
  n_formants,
  window_length,
  pre_emphasis_from
)
}
\arguments{
\item{signal}{Numeric vector, the audio samples}

\item{sr}{Sampling rate in Hz}

\item{time_step}{Time between analysis frames in seconds}

\item{max_formant}{Maximum formant frequency (Hz)}

\item{n_formants}{Number of formants to detect per frame}

\item{window_length}{Analysis window length in seconds}

\item{pre_emphasis_from}{Pre-emphasis frequency in Hz}
}
\value{
A data.table with columns \code{time}, \code{formant_number},
  \code{frequency}, \code{bandwidth} (one row per formant per frame)
}
\description{
Internal formant detection using Burg's method
}
\examples{
set.seed(1)
signal <- sin(2 * pi * 500 * seq(0, 0.5, by = 1 / 16000)) + rnorm(8001, sd = 0.01)
pladdrr:::.detect_formants_burg(signal, sr = 16000, time_step = 0.05,
                                 max_formant = 5500, n_formants = 4,
                                 window_length = 0.025, pre_emphasis_from = 50)
}
\keyword{internal}
