% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-combined.R
\name{plot_spectrogram_pitch}
\alias{plot_spectrogram_pitch}
\title{Plot Spectrogram with Pitch Overlay}
\usage{
plot_spectrogram_pitch(
  spectrogram,
  pitch,
  from_time = NULL,
  to_time = NULL,
  freq_max = 5000,
  pitch_color = "blue",
  pitch_floor = NULL,
  pitch_ceiling = NULL,
  title = "Spectrogram with Pitch",
  ...
)
}
\arguments{
\item{spectrogram}{Spectrogram object}

\item{pitch}{Pitch object}

\item{from_time}{Start time in seconds (NULL = from beginning)}

\item{to_time}{End time in seconds (NULL = to end)}

\item{freq_max}{Maximum frequency to display in Hz (default: 5000)}

\item{pitch_color}{Character. Pitch track color (default: "blue")}

\item{pitch_floor}{Minimum F0 to display in Hz (default: NULL = auto)}

\item{pitch_ceiling}{Maximum F0 to display in Hz (default: NULL = auto)}

\item{title}{Character. Plot title (default: "Spectrogram with Pitch")}

\item{...}{Additional arguments passed to plot methods}
}
\value{
A ggplot2 object
}
\description{
Creates a combined visualization showing a spectrogram with pitch contour
overlaid. This is one of the most common Praat visualizations for voice
 analysis.
}
\examples{
sound <- Sound$create_tone(frequency = 220, duration = 0.5)
spectrogram <- sound$to_spectrogram()
pitch <- sound$to_pitch()

# Basic combined plot
plot_spectrogram_pitch(spectrogram, pitch)

# Customize pitch range
plot_spectrogram_pitch(spectrogram, pitch,
                      pitch_floor = 75, pitch_ceiling = 500,
                      pitch_color = "red")

}
