% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-queries.R
\name{get_intensity_at_times}
\alias{get_intensity_at_times}
\title{Batch Query Intensity Values at Multiple Times}
\usage{
get_intensity_at_times(
  intensity,
  times,
  interpolate = "cubic",
  interpolation = NULL
)
}
\arguments{
\item{intensity}{An Intensity object}

\item{times}{Numeric vector of time points (in seconds)}

\item{interpolate}{Interpolation method: "nearest", "linear", "cubic" (default),
or "sinc70". Kept for backward compatibility; prefer `interpolation`.}

\item{interpolation}{Alias for `interpolate` (consistent with R6 method naming).
When provided, supersedes `interpolate`.}
}
\value{
Numeric vector of intensity values (in dB) at the specified times
}
\description{
Query intensity (amplitude in dB) at multiple time points in a single call.
}
\section{Performance}{

Uses existing optimized C++ code. Expected speedup: **2-3x** compared to R loops.
}

\examples{
\dontrun{
sound <- Sound(system.file("signalfiles/DSI/input/fh1.wav", package = "pladdrr"))
intensity <- sound$to_intensity()
times <- seq(intensity$get_xmin(), intensity$get_xmax(), length.out = 100)
intensities <- get_intensity_at_times(intensity, times)
}

}
