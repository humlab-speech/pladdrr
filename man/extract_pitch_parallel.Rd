% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel-batch.R
\name{extract_pitch_parallel}
\alias{extract_pitch_parallel}
\title{Parallel Pitch Extraction}
\usage{
extract_pitch_parallel(
  files,
  n_cores = NULL,
  pitch_floor = 75,
  pitch_ceiling = 600,
  time_step = 0
)
}
\arguments{
\item{files}{Character vector of file paths}

\item{n_cores}{Integer. Number of cores (default: auto)}

\item{pitch_floor}{Numeric. Minimum pitch in Hz (default: 75)}

\item{pitch_ceiling}{Numeric. Maximum pitch in Hz (default: 600)}

\item{time_step}{Numeric. Time step (default: 0, auto)}
}
\value{
List of Pitch objects
}
\description{
Extract pitch from multiple files in parallel.
Convenience wrapper around analyze_files_parallel.
}
\examples{
audio_dir <- tempfile("audio_")
dir.create(audio_dir)
tone <- Sound$create_tone(frequency = 150, duration = 0.3, sampling_rate = 16000)
tone$save(file.path(audio_dir, "tone1.wav"))
files <- list.files(audio_dir, pattern = "\\\\.wav$", full.names = TRUE)

# n_cores = 1 keeps this a single-process example (CRAN-safe)
pitches <- extract_pitch_parallel(files, n_cores = 1)

}
