================================================================================
AUDIO LOADING MIGRATION SUMMARY
================================================================================

✅ COMPLETED: All audio file loading now uses av package exclusively

CHANGES MADE:
-------------

1. R/sound.R
   - Replaced tuneR::readWave() with av::read_audio_bin()
   - Removed WAV-only restriction
   - Now supports all FFmpeg formats (MP3, FLAC, OGG, M4A, etc.)

2. DESCRIPTION
   - Removed tuneR from Imports
   - Kept av (>= 0.5.0) with Remotes: humlab-speech/av

3. src/sound_fileio_stub.cpp
   - Updated error messages to reference av package
   - Removed references to tuneR, seewave

VERIFICATION:
-------------

✓ No tuneR references in code
✓ No seewave references in code
✓ No other audio loading packages used
✓ Praat C file I/O sources excluded from build:
  - Sound_files.cpp (not in Makevars FON_SRC)
  - LongSound.cpp (not in Makevars FON_SRC)
  - Movie.cpp (not in Makevars FON_SRC)

AUDIO FORMATS SUPPORTED:
------------------------

Via av package (FFmpeg):
  ✓ WAV (all variants)
  ✓ MP3 (MPEG Layer 3)
  ✓ FLAC (lossless)
  ✓ OGG (Vorbis/Opus)
  ✓ M4A/AAC (Apple/iTunes)
  ✓ WMA (Windows Media)
  ✓ AIFF (Apple)
  ✓ 30+ other FFmpeg-supported formats

USAGE EXAMPLES:
---------------

# R6 interface (primary)
sound <- Sound$new("audio.mp3")
sound$save("output.flac", format = "flac")

# Legacy S3 interface
sound <- read_sound("audio.wav")

# Direct av integration
audio_data <- av::read_audio_bin("audio.wav")
sound <- Sound$from_matrix(t(audio_data), sampling_rate = 44100)

================================================================================
Date: 2025-11-24
Package: pladdrr v0.9.9
================================================================================
