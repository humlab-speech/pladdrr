# Get a loaded Rcpp Module by name

Get a loaded Rcpp Module by name

## Usage

``` r
get_module(name)
```

## Arguments

- name:

  Module name (e.g., "pitch_module")

## Value

The loaded Rcpp Module
