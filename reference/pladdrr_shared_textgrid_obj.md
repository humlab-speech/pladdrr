# Shared parameter docs for functions taking a TextGrid object (plain)

Shared parameter docs for functions taking a TextGrid object (plain)

## Usage

``` r
pladdrr_shared_textgrid_obj(x = NULL)
```

## Arguments

- x:

  TextGrid object
