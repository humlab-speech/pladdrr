# Check if Praat interpreter is initialized

Check if Praat interpreter is initialized

## Usage

``` r
praat_initialized()
```

## Value

Logical; TRUE if initialized, FALSE otherwise

## Examples

``` r
praat_init()
praat_initialized()
#> [1] TRUE
```
