# data.table Utility Functions for pladdrr

Internal helper functions for data.table operations. These functions
provide consistent data.table handling across the package.

## Value

This is a documentation-only overview; see the individual functions
(`ensure_datatable`, `df_to_dt`, `dt_empty`, `dt_rbindlist`,
`dt_setkey`, `dt_create`) for their return values.

## Examples

``` r
# See individual functions in R/datatable-utils.R
```
