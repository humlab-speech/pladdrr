# Shared parameter docs for minimum-pitch-in-Hz-only functions

Shared parameter docs for minimum-pitch-in-Hz-only functions

## Usage

``` r
pladdrr_shared_minpitch(pitch_floor = NULL, minimum_pitch = NULL, fmin = NULL)
```

## Arguments

- pitch_floor:

  Minimum pitch (Hz)

- minimum_pitch:

  Minimum pitch (Hz)

- fmin:

  Minimum pitch (Hz)
